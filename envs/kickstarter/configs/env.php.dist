<?php

/**
 * env.php returns the currenct configuration instance and must be in the ignore section of your VCS.
 */

// in this case we return the pre production configs:
return require 'env-local.php';