<?
namespace admin\storage;

class Image
{
    public function createImage($fileId, $filterId = 0)
    {
        // creates an image based on the file id source and the filter id, if filter id is equal 0 there is no filter applied.
        
        // returns imageId;
    }
    
    public function getPath($imageId)
    {
        // get the real full image path to display this file.
    }
}