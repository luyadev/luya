<?php

namespace tests\src\web;

use Yii;
use tests\data\Model;

class ModelTest extends \tests\BaseTest
{
    public function testNgRest()
    {
        $model = new Model();
        
        $model->firstname = ['de' => 'deutsch', 'en' => 'English'];
        
        $r = $model->insert(false);
        var_dump($r);
    }
}